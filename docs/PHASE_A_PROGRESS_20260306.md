# Phase A 进度同步（2026-03-06）

## 1. 本次同步目的
1. 记录当前已落地能力，方便下次快速接手。
2. 明确“可验收范围”和“下一步开发顺序”。

---

## 2. 已落地能力（代码已实现）

## 2.1 权限体系
1. 模块级权限 + 动作级权限并存。
2. 关键动作已接入动作码校验：
   `order.confirm_delivery` / `order.mark_returned` / `order.force_cancel` /
   `transfer.recommend` / `transfer.create_task` / `transfer.complete_task` /
   `transfer.cancel_task` / `inventory.init_units` 等。
3. 页面按钮可见性已按动作权限控制。

## 2.2 结构化审计
1. 统一审计结构：`summary / before / after / changed_fields / extra`。
2. 审计服务统一入口：`AuditService.log_with_diff(...)`。
3. 已接入模块：
   - 订单服务（创建/编辑/确认/发货/归还/完成/取消）
   - 转寄关键动作（重推/生成/完成/取消，含 API 创建）
   - 采购与部件服务（状态流转、出入库、调整）
   - 系统设置保存
   - 在外库存初始化
   - 审计中间件（请求级）
4. 代码守卫测试：防止业务代码回退到直接 `AuditLog.objects.create(...)`。

## 2.3 审计查询与导出
1. 审计页面支持：
   - 日期、操作类型、模块、关键词
   - 仅结构化、仅有字段变更、仅风险动作
   - 来源筛选（app/api/middleware/plain）
   - 排序（时间/变更字段数）
2. 审计页面展示：
   - 摘要
   - 变更字段标签
   - before/after/extra 折叠查看
   - 当前筛选结果统计概览
3. CSV 导出支持：
   - 继承当前筛选条件
   - 分列输出 `Before/After/Extra(JSON)`、变更字段、变更字段数量、来源

## 2.4 角色看板与风险入口
1. 工作台支持按角色动态渲染：
   - 指标卡 `focus_cards`
   - 快捷操作 `quick_actions`
2. 风险入口支持跳转到筛选后的目标列表：
   - 待处理订单 -> 订单中心
   - 待执行转寄 -> 转寄中心任务面板
   - 低库存部件 -> 部件库存（`low=1`）
3. API 已返回角色看板完整 payload（含 `risk_entries`），可供后续前端组件化改造复用。

## 2.5 在外库存健康评分（生命周期可视化）
1. 增加单套健康分计算（0~100）与健康等级（健康/关注/风险）。
2. 看板展示：
   - 总览：在外健康均分
   - SKU总表：SKU健康均分
   - 单套明细：健康分与等级标签
3. 导出能力同步：在外库存 CSV 增加 `健康分/健康等级` 字段。

---

## 3. 当前测试状态
1. 全量测试通过：当前 `72` 条。
2. 新增测试覆盖：
   - 审计 diff 写入（订单/转寄/设置）
   - 审计导出筛选
   - 来源筛选
   - 中间件结构化写入
   - 守卫测试（禁止直接写 AuditLog）

---

## 4. 当前可验收清单
1. 任意关键动作可在审计页看到结构化前后变化。
2. 可按“风险动作 + 有字段变更 + 来源=api/middleware”快速定位问题。
3. 导出的 CSV 可直接用于外部二次分析（Excel/BI）。

---

## 5. 下一步建议（顺序执行）
1. A-06：维护页面/API统计口径对照清单文档（已新增初版）。
2. A-07：按清单修复不一致口径并补回归测试（已新增页面/API一致性测试）。
3. A-08：角色看板V1数据层（只读指标接口优先）。
4. A-09：角色看板V1前端（已完成基础版，含角色卡片与角色快捷操作）。
5. A-10：风险提示轻量版（已完成基础版，风险标签可跳转到对应列表筛选）。

---

## 6. 注意事项
1. 本阶段改动集中在权限与审计，不应破坏订单/转寄/库存主链路。
2. 新增审计字段优先写入 `extra`，避免影响既有查询结构。
3. 文档与测试需同步更新，避免“代码有、文档无”。
